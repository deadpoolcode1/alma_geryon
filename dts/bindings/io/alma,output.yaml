description: Alma Output IO

compatible: "alma,output"

include: base.yaml

properties:
    status:
      required: true

    control-gpios:
      type: phandle-array
      required: true
      description: |
        GPIO output
