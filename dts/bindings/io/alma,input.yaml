description: Input IO

compatible: "alma,input"

include: base.yaml

properties:
    status:
      required: true

    control-gpios:
      type: phandle-array
      required: true
      description: |
        GPIO input
